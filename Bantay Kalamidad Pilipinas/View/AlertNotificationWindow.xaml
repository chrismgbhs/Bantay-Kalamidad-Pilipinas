<Window x:Class="Bantay_Kalamidad_Pilipinas.Views.AlertNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alert"
        Width="380" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="CloseBtn" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize"        Value="14"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="4">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Fade-in animation -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="RootBorder"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.3"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border x:Name="RootBorder"
            CornerRadius="8" Opacity="0"
            Margin="12"
            Background="#1E1E2E"
            BorderThickness="1.5">

        <Border.BorderBrush>
            <SolidColorBrush x:Name="AccentBrush" Color="#E05C5C"/>
        </Border.BorderBrush>

        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.6" ShadowDepth="4"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Colored top bar -->
            <Border Grid.Row="0" Height="4" CornerRadius="8,8,0,0">
                <Border.Background>
                    <SolidColorBrush x:Name="TopBarBrush" Color="#E05C5C"/>
                </Border.Background>
            </Border>

            <!-- Header: icon + source + timestamp + close -->
            <Grid Grid.Row="1" Margin="14,10,10,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="IconText"
                           Grid.Column="0"
                           Text="⚠️" FontSize="18"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="SourceLabel"
                               Text="PAGASA"
                               FontSize="10" FontWeight="SemiBold"
                               Foreground="#AAAACC"/>
                    <TextBlock x:Name="TimestampLabel"
                               Text="Just now"
                               FontSize="10" Foreground="#666688"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Style="{StaticResource CloseBtn}"
                        Content="✕"
                        Click="CloseButton_Click"
                        VerticalAlignment="Top"/>
            </Grid>

            <!-- Title -->
            <TextBlock x:Name="TitleText"
                       Grid.Row="2"
                       Margin="14,0,14,6"
                       TextWrapping="Wrap"
                       FontSize="13" FontWeight="Bold"
                       Foreground="White"/>

            <!-- Body -->
            <TextBlock x:Name="BodyText"
                       Grid.Row="3"
                       Margin="14,0,14,14"
                       TextWrapping="Wrap"
                       MaxHeight="80"
                       FontSize="11" Foreground="#CCCCCC"
                       TextTrimming="CharacterEllipsis"/>
        </Grid>
    </Border>
</Window>
