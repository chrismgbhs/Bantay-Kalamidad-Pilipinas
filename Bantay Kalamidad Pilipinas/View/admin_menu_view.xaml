<UserControl x:Class="Bantay_Kalamidad_Pilipinas.View.admin_menu_view"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Viewbox Stretch="Uniform">
        <Grid Width="1100" Height="750">

            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0,-40,0,0">

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,95">

                    <Image Source="/Images/bkp_logo.png"
                           Width="75"
                           Height="75"
                           Margin="0,0,18,0"/>

                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Bantay Kalamidad Pilipinas"
                                   FontSize="28"
                                   FontWeight="Black"/>
                        <TextBlock Text="Disaster Response and Relief Coordination System"
                                   FontSize="20"
                                   FontWeight="Bold"/>
                    </StackPanel>

                </StackPanel>

                <TextBlock Text="ADMIN MENU"
                           FontSize="22"
                           FontWeight="Black"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,35"/>

                <Button Content="RESCUE"
                        Width="280"
                        Height="70"
                        Background="#E51612"
                        BorderBrush="#7A0606"
                        BorderThickness="3"
                        Foreground="White"
                        FontSize="18"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Command="{Binding OpenAdminRescueDashboardCommand}"
                        Margin="0,0,0,40">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="DONATION"
                        Width="280"
                        Height="70"
                        Background="#246F8F"
                        BorderBrush="#07314E"
                        BorderThickness="3"
                        Foreground="White"
                        FontSize="18"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Command="{Binding OpenAdminDonationDashboardCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>

        </Grid>
    </Viewbox>
</UserControl>